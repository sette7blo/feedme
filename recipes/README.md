# Recipe JSON files stored here (schema.org/Recipe format)
# One file per recipe, named by slug: spaghetti-carbonara.json
# This folder is the source of truth — SQLite is rebuilt from these files.
# Back this folder up. The database (chef.db) is disposable.
